void atualizaTemperatura(int temp) {
    switch (temp)
    {
    case 1:
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS --------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        break;

    case 2:

        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;

    case 3:

        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS ------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;

    case 4:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -----------------------------------------------------------
        
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

    case 5:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS ----------------------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;

    case 6:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS ----------------------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;

   
    case 7:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS ------------------------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;

    case 8:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS ------------------------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 9:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS ------------------------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 10:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;

    case 11:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;

    case 12:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS ------------------------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 13:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS ------------------------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 14:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show(); 
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS ------------------------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;

    case 15:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 16:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
        
    case 17:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;
        
    case 18:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 19:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
        
    case 20:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;
        
    case 21:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;

    case 22:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 23:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
        
    case 24:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
    case 25:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
        
    case 26:
        

        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 27:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;
        
    case 28:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
        
    case 29:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 30:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;

    case 31:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;
        
    case 32:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
    case 33:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 34:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
        
    case 35:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
        
    case 36:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;

    case 37:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;

    case 38:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
        
    case 39:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;

    case 40:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;

    case 41:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        break;

    case 42:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
        
    case 43:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        break;
    
    case 44:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i <  65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;

    case 45:
        
        //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;

    case 46:
        
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;

    case 47:
    
    //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        break;

    case 48:
    
    //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;

    case 49:
    
    //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS -------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        break;
        
    case 50:
    
    //Primeiro segmento
        for (int i = 0; i < 5; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 5; i < 10; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 10; i < 15; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 15; i < 20; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 20; i < 25; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 25; i < 30; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 30; i < 35; i++)
        {
            leds[i] = CRGB(R, G, B);
            FastLED.show();
        }

        // SEGUNDO 7 SEGMENTOS ------------------------------------------------------------
        
        //Primeiro segmento
        for (int i = 35; i < 40; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        
        //Segundo segmento
        for (int i = 40; i < 45; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Terceiro segmento
        for (int i = 45; i < 50; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quarto segmento
        for (int i = 50; i < 55; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Quinto segmento
        for (int i = 55; i < 60; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sexto segmento
        for (int i = 60; i < 65; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }

        //Sétimo segmento
        for (int i = 65; i < 70; i++)
        {
            leds[i] = CRGB(R_d, G_d, B_d);
            FastLED.show();
        }
        break;
    
    default:
        break;
    }
}